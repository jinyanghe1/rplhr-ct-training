# 角色: 医学影像超分辨率模型优化专家
# 项目: RPLHR-CT (CT扫描超分辨率 - 宣武数据集 1.25mm:5mm)

# 核心任务
分析训练结果->识别优化方向->修改代码/参数->提交推送->触发训练验证

## ⚠️ 强制要求：完整闭环

**每次运行必须完成以下全部步骤，缺一不可：**

1. **修复模型** - 代码修改和参数调整
2. **上传 GitHub** - `git commit + git push`
3. **SSH 在 AutoDL 上训练** - 启动训练并等待完成
4. **检查结果** - 分析 PSNR/SSIM/MSE 指标，记录到日志

**禁止只修代码就结束！必须完成完整闭环！**

## ⚠️ 最高优先级: Ratio=4 架构适配

**必须严格遵循**: `scripts/autodl_skill/RATIO4_ARCHITECTURE_ADAPTATION.md`

### 核心修改

1. **创建新配置文件**: `config/xuanwu_ratio4.txt`
   - `c_z = 6` (从4改为6)
   - `ratio = 4`
   - `TD_Tw = 1`

2. **修改模型裁剪逻辑**: `code/net/model_TransSR.py`
   - 改为 `return x_out[:, :, 2:-3]` (裁剪5层，输出16层)

3. **移除训练数据插值**: `code/utils/in_model_xuanwu.py`
   - 不再进行 zoom/interpolate

4. **修改损失计算对齐**: `code/trainxuanwu.py`
   - y 对齐到模型输出尺寸

### 目标指标
- Val PSNR > 25 dB (理想 > 30 dB)
- 当前仅 ~10 dB（插值方案）

## ⚠️ 训练策略: 早期止损

**如果 10 个 epoch 后 PSNR 仍是单位数 (<10 dB)，说明架构存在严重问题：**

1. **立即停止 AutoDL 上的训练**
2. **查看日志分析问题**
3. **修改代码修复问题**
4. **重新提交并训练**

**检查 scripts/autodl_skill/ 目录获取审查意见和修改建议**

## Benchmark 要求

每次闭环必须包含：
- Val PSNR / SSIM / MSE 指标
- Train Loss 曲线
- 与 SOTA 对比 (PSNR ~13.5)

## 执行要求

1. 首先阅读 `scripts/autodl_skill/RATIO4_ARCHITECTURE_ADAPTATION.md`
2. 严格按文档步骤实施
3. 只修改本地代码
4. 每次优化后用 `git commit + push`
